---
title: Electron 界面基础
date: 2026-03-04
tags:
  - electron
  - menu
  - tray
difficulty: ⭐
status: 📋 待学习
---

# Electron 界面基础

## 🎯 学习目标

- 掌握菜单栏 (Menu) 的创建和配置
- 学会系统托盘 (Tray) 的使用
- 理解快捷键 (Accelerator) 的注册方式
- 了解原生对话框 (Dialog) 的使用

---

## 📖 知识概览

```
┌─────────────────────────────────────────────────────────────┐
│                    Electron 界面组件                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 菜单栏 (Menu)                                        │   │
│  │ ├── 应用菜单 (Application Menu)                      │   │
│  │ ├── 上下文菜单 (Context Menu)                        │   │
│  │ └── Dock 菜单 (macOS)                               │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 系统托盘 (Tray)                                      │   │
│  │ ├── 托盘图标                                         │   │
│  │ ├── 托盘菜单                                         │   │
│  │ └── 气泡通知                                         │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 快捷键 (Accelerator)                                 │   │
│  │ ├── 全局快捷键                                       │   │
│  │ └── 局部快捷键                                       │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 对话框 (Dialog)                                      │   │
│  │ ├── 文件选择对话框                                   │   │
│  │ ├── 消息对话框                                       │   │
│  │ └── 保存对话框                                       │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🍽️ 菜单栏 (Menu)

### 1. 创建应用菜单

```javascript
const { app, Menu } = require('electron')

const isMac = process.platform === 'darwin'

const template = [
  // { role: 'appMenu' } - macOS 应用菜单
  ...(isMac ? [{
    label: app.name,
    submenu: [
      { role: 'about' },
      { type: 'separator' },
      { role: 'services' },
      { type: 'separator' },
      { role: 'hide' },
      { role: 'hideOthers' },
      { role: 'unhide' },
      { type: 'separator' },
      { role: 'quit' }
    ]
  }] : []),

  // 文件菜单
  {
    label: '文件',
    submenu: [
      {
        label: '新建',
        accelerator: 'CmdOrCtrl+N',
        click: () => createNewDocument()
      },
      {
        label: '打开',
        accelerator: 'CmdOrCtrl+O',
        click: async () => {
          const { dialog } = require('electron')
          const result = await dialog.showOpenDialog({
            filters: [{ name: 'Documents', extensions: ['txt', 'md'] }]
          })
          if (!result.canceled) {
            openFile(result.filePaths[0])
          }
        }
      },
      { type: 'separator' },
      {
        label: '保存',
        accelerator: 'CmdOrCtrl+S',
        click: () => saveDocument()
      },
      { type: 'separator' },
      {
        label: '退出',
        accelerator: 'CmdOrCtrl+Q',
        role: 'quit'
      }
    ]
  },

  // 编辑菜单
  {
    label: '编辑',
    submenu: [
      { role: 'undo', label: '撤销' },
      { role: 'redo', label: '重做' },
      { type: 'separator' },
      { role: 'cut', label: '剪切' },
      { role: 'copy', label: '复制' },
      { role: 'paste', label: '粘贴' },
      { role: 'delete', label: '删除' },
      { type: 'separator' },
      { role: 'selectAll', label: '全选' }
    ]
  },

  // 视图菜单
  {
    label: '视图',
    submenu: [
      { role: 'reload', label: '刷新' },
      { role: 'forceReload', label: '强制刷新' },
      { role: 'toggleDevTools', label: '开发者工具' },
      { type: 'separator' },
      { role: 'resetZoom', label: '重置缩放' },
      { role: 'zoomIn', label: '放大' },
      { role: 'zoomOut', label: '缩小' },
      { type: 'separator' },
      { role: 'togglefullscreen', label: '全屏' }
    ]
  },

  // 窗口菜单
  {
    label: '窗口',
    submenu: [
      { role: 'minimize', label: '最小化' },
      { role: 'zoom', label: '缩放' },
      ...(isMac ? [
        { type: 'separator' },
        { role: 'front', label: '前置全部窗口' },
        { type: 'separator' },
        { role: 'window', label: '窗口' }
      ] : [
        { role: 'close', label: '关闭' }
      ])
    ]
  },

  // 帮助菜单
  {
    role: 'help',
    label: '帮助',
    submenu: [
      {
        label: '学习更多',
        click: async () => {
          const { shell } = require('electron')
          await shell.openExternal('https://electronjs.org')
        }
      },
      {
        label: '关于',
        click: () => showAboutDialog()
      }
    ]
  }
]

const menu = Menu.buildFromTemplate(template)
Menu.setApplicationMenu(menu)
```

### 2. 上下文菜单 (右键菜单)

```javascript
const { Menu } = require('electron')

// 创建上下文菜单
const contextMenu = Menu.buildFromTemplate([
  { label: '复制', role: 'copy' },
  { label: '粘贴', role: 'paste' },
  { type: 'separator' },
  {
    label: '自定义操作',
    click: () => console.log('自定义操作被点击')
  },
  { type: 'separator' },
  { label: '检查元素', role: 'inspectElement' }
])

// 在窗口中监听右键事件
mainWindow.webContents.on('context-menu', (event, params) => {
  // 根据上下文动态调整菜单
  if (params.selectionText) {
    contextMenu.popup(mainWindow, params.x, params.y)
  }
})
```

### 3. 动态菜单

```javascript
// 动态更新菜单项
function updateMenu(recentFiles) {
  const fileMenu = template.find(item => item.label === '文件')
  const recentFilesMenu = {
    label: '最近打开',
    submenu: recentFiles.map(file => ({
      label: file.name,
      click: () => openFile(file.path)
    }))
  }

  // 插入到文件菜单
  const openIndex = fileMenu.submenu.findIndex(item => item.label === '打开')
  fileMenu.submenu.splice(openIndex + 1, 0, recentFilesMenu)

  // 重建菜单
  const menu = Menu.buildFromTemplate(template)
  Menu.setApplicationMenu(menu)
}
```

---

## 🖼️ 系统托盘 (Tray)

### 1. 基本托盘

```javascript
const { Tray, Menu, nativeImage } = require('electron')
const path = require('path')

let tray

function createTray() {
  // 创建图标
  const iconPath = path.join(__dirname, 'assets', 'tray-icon.png')
  const icon = nativeImage.createFromPath(iconPath)

  // macOS 需要设置模板图标（自动适配深色/浅色模式）
  const trayIcon = process.platform === 'darwin'
    ? icon.resize({ width: 16, height: 16 }).setTemplateImage(true)
    : icon

  tray = new Tray(trayIcon)

  // 设置托盘菜单
  const contextMenu = Menu.buildFromTemplate([
    { label: '显示窗口', click: () => mainWindow.show() },
    { label: '新建笔记', click: () => createNewNote() },
    { type: 'separator' },
    {
      label: '状态',
      submenu: [
        { label: '在线', type: 'radio', checked: true },
        { label: '忙碌', type: 'radio' },
        { label: '离开', type: 'radio' }
      ]
    },
    { type: 'separator' },
    { label: '设置', click: () => openSettings() },
    { label: '关于', click: () => showAbout() },
    { type: 'separator' },
    { label: '退出', click: () => app.quit() }
  ])

  tray.setToolTip('我的应用')
  tray.setContextMenu(contextMenu)

  // 点击托盘图标
  tray.on('click', () => {
    mainWindow.isVisible() ? mainWindow.hide() : mainWindow.show()
  })

  // 双击托盘图标
  tray.on('double-click', () => {
    mainWindow.show()
    mainWindow.focus()
  })
}
```

### 2. 托盘通知

```javascript
// 显示气泡通知
tray.displayBalloon({
  title: '新消息',
  content: '您有一条新消息',
  iconType: 'info'  // 'none' | 'info' | 'warning' | 'error'
})

// macOS: 设置标题
tray.setTitle('3 条未读消息')
```

### 3. 动态托盘图标

```javascript
// 创建带数字的托盘图标
function updateTrayIcon(count) {
  const canvas = createCanvas(16, 16)
  const ctx = canvas.getContext('2d')

  // 绘制背景
  ctx.fillStyle = count > 0 ? '#e74c3c' : '#3498db'
  ctx.beginPath()
  ctx.arc(8, 8, 8, 0, Math.PI * 2)
  ctx.fill()

  // 绘制数字
  if (count > 0) {
    ctx.fillStyle = 'white'
    ctx.font = 'bold 10px Arial'
    ctx.textAlign = 'center'
    ctx.textBaseline = 'middle'
    ctx.fillText(count > 9 ? '9+' : count.toString(), 8, 8)
  }

  const image = nativeImage.createFromDataURL(canvas.toDataURL())
  tray.setImage(image)
}
```

---

## ⌨️ 快捷键 (Accelerator)

### 1. 菜单快捷键

```javascript
{
  label: '保存',
  accelerator: 'CmdOrCtrl+S',  // macOS: Cmd+S, Windows: Ctrl+S
  click: () => saveDocument()
}
```

### 2. 全局快捷键

```javascript
const { app, globalShortcut } = require('electron')

app.whenReady().then(() => {
  // 注册全局快捷键
  globalShortcut.register('CommandOrControl+Shift+I', () => {
    console.log('快捷键被触发')
    mainWindow.webContents.toggleDevTools()
  })

  // 注册多个快捷键
  globalShortcut.register('CommandOrControl+Shift+S', () => {
    // 截图功能
    captureScreen()
  })
})

// 应用退出时注销快捷键
app.on('will-quit', () => {
  globalShortcut.unregisterAll()
})
```

### 3. 快捷键格式

```
┌─────────────────────────────────────────────────────────────┐
│                     快捷键格式                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  修饰键:                                                     │
│  - Command / Cmd        (macOS)                             │
│  - Control / Ctrl       (Windows/Linux)                     │
│  - CommandOrControl     (跨平台: macOS用Cmd, 其他用Ctrl)     │
│  - Alt / Option                                            │
│  - Shift                                                   │
│  - Super / Meta         (Windows键 / Linux Super键)         │
│                                                             │
│  功能键:                                                     │
│  - F1 到 F24                                               │
│  - Plus, Space, Tab, Backspace, Delete                     │
│  - Insert, Return, Enter, Up, Down, Left, Right            │
│  - Home, End, PageUp, PageDown, Escape, Esc               │
│                                                             │
│  示例:                                                       │
│  - "CommandOrControl+A"                                    │
│  - "CommandOrControl+Shift+Z"                              │
│  - "Alt+Return"                                            │
│  - "F11"                                                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 💬 对话框 (Dialog)

### 1. 文件选择对话框

```javascript
const { dialog } = require('electron')

// 打开文件
async function openFile() {
  const result = await dialog.showOpenDialog(mainWindow, {
    title: '选择文件',
    defaultPath: app.getPath('documents'),
    filters: [
      { name: '文本文件', extensions: ['txt', 'md'] },
      { name: '所有文件', extensions: ['*'] }
    ],
    properties: [
      'openFile',           // 选择文件
      'multiSelections',    // 多选
      'showHiddenFiles'     // 显示隐藏文件
    ]
  })

  if (!result.canceled) {
    return result.filePaths
  }
  return null
}

// 选择文件夹
async function selectFolder() {
  const result = await dialog.showOpenDialog(mainWindow, {
    properties: ['openDirectory', 'createDirectory']
  })

  if (!result.canceled) {
    return result.filePaths[0]
  }
  return null
}
```

### 2. 保存对话框

```javascript
async function saveFile() {
  const result = await dialog.showSaveDialog(mainWindow, {
    title: '保存文件',
    defaultPath: 'untitled.txt',
    filters: [
      { name: '文本文件', extensions: ['txt'] },
      { name: 'Markdown', extensions: ['md'] }
    ]
  })

  if (!result.canceled) {
    return result.filePath
  }
  return null
}
```

### 3. 消息对话框

```javascript
// 显示消息
async function showMessage() {
  const result = await dialog.showMessageBox(mainWindow, {
    type: 'info',           // 'none' | 'info' | 'error' | 'question' | 'warning'
    title: '提示',
    message: '文件已保存',
    detail: '文件已成功保存到指定位置',
    buttons: ['确定', '打开文件夹'],
    defaultId: 0,
    cancelId: 0
  })

  if (result.response === 1) {
    // 点击了"打开文件夹"
    shell.showItemInFolder(filePath)
  }
}

// 确认对话框
async function confirmDelete() {
  const result = await dialog.showMessageBox(mainWindow, {
    type: 'warning',
    title: '确认删除',
    message: '确定要删除这个文件吗？',
    detail: '此操作无法撤销',
    buttons: ['取消', '删除'],
    defaultId: 0,
    cancelId: 0,
    checkboxLabel: '不再询问'
  })

  return {
    confirmed: result.response === 1,
    dontAskAgain: result.checkboxChecked
  }
}
```

### 4. 错误对话框

```javascript
// 显示错误
dialog.showErrorBox('出错了', '无法连接到服务器')
```

---

## 📝 实践练习

- [ ] 创建一个完整的应用菜单（文件、编辑、视图、帮助）
- [ ] 实现带动态图标的系统托盘
- [ ] 注册全局快捷键实现窗口显示/隐藏
- [ ] 实现文件打开和保存对话框

---

## 🔗 相关链接

- [[03-窗口管理]] - 窗口创建和管理
- [Menu API 文档](https://www.electronjs.org/docs/latest/api/menu)
- [Tray API 文档](https://www.electronjs.org/docs/latest/api/tray)
- [Dialog API 文档](https://www.electronjs.org/docs/latest/api/dialog)

---

#tech/electron/ui
