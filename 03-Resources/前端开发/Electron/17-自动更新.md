---
title: Electron 自动更新
date: 2026-03-04
tags:
  - electron
  - update
difficulty: ⭐⭐⭐
status: 📋 待学习
---

# Electron 自动更新

## 🎯 学习目标

- 掌握 electron-updater 的使用
- 学会搭建更新服务器
- 了解增量更新和更新策略

---

## 🔄 自动更新原理

```
┌─────────────────────────────────────────────────────────────┐
│                    自动更新流程                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 检查更新                                                 │
│     App ──GET──→ Server/latest.yml                          │
│                                                             │
│  2. 对比版本                                                 │
│     App: v1.0.0  vs  Server: v1.1.0                         │
│                                                             │
│  3. 下载更新                                                 │
│     App ←─────── Server/new-version.exe                     │
│                                                             │
│  4. 安装更新                                                 │
│     - Windows: 替换文件后重启                                │
│     - macOS: 替换 .app 后重启                                │
│     - Linux: 替换 AppImage                                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📦 electron-updater

### 1. 安装配置

```bash
npm install electron-updater
```

```json
// package.json
{
  "build": {
    "publish": {
      "provider": "github",
      "owner": "your-username",
      "repo": "your-repo"
    }
  }
}
```

### 2. 基本使用

```javascript
// main.js
const { app, BrowserWindow } = require('electron')
const { autoUpdater } = require('electron-updater')
const log = require('electron-log')

// 配置日志
autoUpdater.logger = log
autoUpdater.logger.transports.file.level = 'info'

// 配置更新服务器
autoUpdater.setFeedURL({
  provider: 'github',
  owner: 'your-username',
  repo: 'your-repo'
})

// 检查更新
function checkForUpdates() {
  autoUpdater.checkForUpdatesAndNotify()
}

// 自动检查更新
app.whenReady().then(() => {
  // 启动时检查
  checkForUpdates()

  // 定期检查（每小时）
  setInterval(checkForUpdates, 60 * 60 * 1000)
})

// 事件处理
autoUpdater.on('checking-for-update', () => {
  log.info('Checking for update...')
})

autoUpdater.on('update-available', (info) => {
  log.info('Update available:', info.version)
})

autoUpdater.on('update-not-available', (info) => {
  log.info('Update not available.')
})

autoUpdater.on('error', (err) => {
  log.error('Error in auto-updater:', err)
})

autoUpdater.on('download-progress', (progressObj) => {
  let log_message = `Download speed: ${progressObj.bytesPerSecond}`
  log_message += ` - Downloaded ${progressObj.percent}%`
  log_message += ` (${progressObj.transferred}/${progressObj.total})`
  log.info(log_message)
})

autoUpdater.on('update-downloaded', (info) => {
  log.info('Update downloaded:', info.version)
  // 提示用户安装
  dialog.showMessageBox({
    type: 'info',
    title: '应用更新',
    message: '新版本已下载，是否立即安装？',
    buttons: ['立即安装', '稍后安装']
  }).then((result) => {
    if (result.response === 0) {
      autoUpdater.quitAndInstall()
    }
  })
})
```

### 3. 完整更新模块

```javascript
// updater.js
const { autoUpdater } = require('electron-updater')
const { dialog, BrowserWindow } = require('electron')
const log = require('electron-log')

class Updater {
  constructor(mainWindow) {
    this.mainWindow = mainWindow
    this.setupAutoUpdater()
  }

  setupAutoUpdater() {
    // 日志配置
    autoUpdater.logger = log
    autoUpdater.logger.transports.file.level = 'info'

    // 不自动下载
    autoUpdater.autoDownload = false
    // 不自动安装
    autoUpdater.autoInstallOnAppQuit = true

    // 事件监听
    autoUpdater.on('checking-for-update', () => {
      this.sendStatus('checking')
    })

    autoUpdater.on('update-available', (info) => {
      this.sendStatus('available', info)
      this.promptDownload(info)
    })

    autoUpdater.on('update-not-available', (info) => {
      this.sendStatus('not-available', info)
    })

    autoUpdater.on('download-progress', (progress) => {
      this.sendProgress(progress)
    })

    autoUpdater.on('update-downloaded', (info) => {
      this.sendStatus('downloaded', info)
      this.promptInstall(info)
    })

    autoUpdater.on('error', (error) => {
      this.sendStatus('error', { error: error.message })
    })
  }

  check() {
    autoUpdater.checkForUpdates()
  }

  download() {
    autoUpdater.downloadUpdate()
  }

  install() {
    autoUpdater.quitAndInstall()
  }

  sendStatus(status, data = {}) {
    this.mainWindow?.webContents.send('update-status', { status, ...data })
  }

  sendProgress(progress) {
    this.mainWindow?.webContents.send('update-progress', {
      percent: Math.round(progress.percent),
      bytesPerSecond: progress.bytesPerSecond,
      transferred: progress.transferred,
      total: progress.total
    })
  }

  async promptDownload(info) {
    const result = await dialog.showMessageBox(this.mainWindow, {
      type: 'info',
      title: '发现新版本',
      message: `发现新版本 ${info.version}`,
      detail: '是否立即下载更新？',
      buttons: ['下载', '稍后提醒'],
      defaultId: 0,
      cancelId: 1
    })

    if (result.response === 0) {
      this.download()
    }
  }

  async promptInstall(info) {
    const result = await dialog.showMessageBox(this.mainWindow, {
      type: 'info',
      title: '更新已就绪',
      message: `版本 ${info.version} 已下载完成`,
      detail: '是否立即安装更新？应用将重启。',
      buttons: ['立即安装', '稍后安装'],
      defaultId: 0,
      cancelId: 1
    })

    if (result.response === 0) {
      this.install()
    }
  }
}

module.exports = Updater
```

**使用更新模块**

```javascript
// main.js
const Updater = require('./updater')

let mainWindow
let updater

app.whenReady().then(() => {
  mainWindow = new BrowserWindow({...})
  updater = new Updater(mainWindow)

  // 检查更新
  updater.check()
})

// IPC 接口
ipcMain.handle('update:check', () => updater.check())
ipcMain.handle('update:download', () => updater.download())
ipcMain.handle('update:install', () => updater.install())
```

---

## 🖥️ 更新服务器

### 1. GitHub Releases

```json
// package.json
{
  "build": {
    "publish": {
      "provider": "github",
      "owner": "your-username",
      "repo": "your-repo",
      "releaseType": "release"
    }
  }
}
```

### 2. 自建服务器

```javascript
// 使用静态文件服务器
// 目录结构:
// updates/
// ├── latest.yml           # Windows
// ├── latest-mac.yml       # macOS
// ├── latest-linux.yml     # Linux
// └── myapp-1.0.0.exe      # 安装包

// 配置
autoUpdater.setFeedURL({
  provider: 'generic',
  url: 'https://updates.example.com'
})
```

**latest.yml 示例**

```yaml
version: 1.0.1
files:
  - url: myapp-1.0.1.exe
    sha512: xxxxx
    size: 50000000
path: myapp-1.0.1.exe
sha512: xxxxx
releaseDate: '2024-01-01T00:00:00.000Z'
```

### 3. S3/CDN

```javascript
autoUpdater.setFeedURL({
  provider: 's3',
  bucket: 'my-app-updates',
  region: 'us-east-1',
  path: 'releases/'
})
```

### 4. 私有服务器 + 签名

```javascript
// 使用私有 GitHub 仓库
autoUpdater.setFeedURL({
  provider: 'github',
  owner: 'your-org',
  repo: 'your-private-repo',
  private: true,
  token: process.env.GH_TOKEN
})
```

---

## 🎯 更新策略

### 1. 灰度发布

```javascript
class Updater {
  async checkWithCanary() {
    const info = await autoUpdater.checkForUpdates()

    if (info.updateInfo) {
      // 检查是否在灰度名单
      const userId = this.getUserId()
      const canaryPercent = info.updateInfo.stagingPercentage || 0

      if (this.isInCanary(userId, canaryPercent)) {
        // 下载更新
        await autoUpdater.downloadUpdate()
      } else {
        log.info('User not in canary rollout')
      }
    }
  }

  isInCanary(userId, percentage) {
    // 使用用户 ID 计算是否在灰度范围内
    const hash = this.hashUserId(userId)
    return (hash % 100) < percentage
  }

  hashUserId(userId) {
    // 简单哈希
    let hash = 0
    for (let i = 0; i < userId.length; i++) {
      hash = ((hash << 5) - hash) + userId.charCodeAt(i)
      hash = hash & hash
    }
    return Math.abs(hash)
  }
}
```

### 2. 强制更新

```javascript
autoUpdater.on('update-downloaded', (info) => {
  // 检查是否强制更新
  if (info.mandatory) {
    dialog.showMessageBox({
      type: 'warning',
      title: '必须更新',
      message: '此更新包含重要安全修复，必须安装。',
      buttons: ['立即安装']
    }).then(() => {
      autoUpdater.quitAndInstall()
    })
  } else {
    // 可选更新
    this.promptInstall(info)
  }
})
```

### 3. 增量更新

```javascript
// electron-builder 配置
{
  "build": {
    "nsis": {
      "differentialPackage": true
    }
  }
}

// 启用增量下载
autoUpdater.allowPrerelease = false
autoUpdater.fullChangelog = true
```

---

## 📊 更新状态 UI

```javascript
// preload.js
contextBridge.exposeInMainWorld('updater', {
  onStatus: (callback) => {
    ipcRenderer.on('update-status', (event, data) => callback(data))
  },
  onProgress: (callback) => {
    ipcRenderer.on('update-progress', (event, data) => callback(data))
  },
  check: () => ipcRenderer.invoke('update:check'),
  download: () => ipcRenderer.invoke('update:download'),
  install: () => ipcRenderer.invoke('update:install')
})

// renderer.js
window.updater.onStatus((data) => {
  console.log('Update status:', data.status)
  updateUI(data)
})

window.updater.onProgress((data) => {
  console.log('Download progress:', data.percent + '%')
  updateProgressBar(data.percent)
})
```

---

## 📝 实践练习

- [ ] 集成 electron-updater
- [ ] 实现更新检查和下载
- [ ] 搭建更新服务器
- [ ] 实现灰度发布策略

---

## 🔗 相关链接

- [[16-打包发布]] - 打包配置
- [electron-updater 文档](https://www.electron.build/auto-update)
- [GitHub Releases](https://docs.github.com/en/repositories/releasing-projects-on-github)

---

#tech/electron/update
